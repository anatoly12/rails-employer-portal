<div class="min-h-screen bg-white relative">
  <div class="pb-40 md:pb-24">
    <%= render "common/header" %>
    <main>
      <%= yield %>
    </main>
  </div>
  <%= render "common/footer" %>
</div>
